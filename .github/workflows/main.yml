name: Lore Origins update

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  linux-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.4
      - run: |
          haxe -version
          haxelib install hxnodejs
          haxelib setup ~/haxelib
      - name: Install hxcpp 4.3.2
        run: |
          haxelib install hxcpp 4.3.2
      - name: Install lime 8.2.2
        run: |
          haxelib install lime 8.2.2
      - name: Install openfl 9.4.1
        run: |
          haxelib install openfl 9.4.1
      - name: Install and Setup flixel 6.1.0
        run: |
          haxelib --never install flixel 6.1.0
          haxelib run lime setup flixel
          haxelib run lime setup
      - name: Install hxCodec 3.0.2
        run: |
          haxelib install hxCodec 3.0.2
      - name: Install flixel tools and others
        run: |
          haxelib install flixel-tools 1.5.1
          haxelib set flixel-tools 1.5.1
          haxelib install flixel-ui 2.6.3
          haxelib set flixel-ui 2.6.3
          haxelib install flixel-addons 3.3.2
          haxelib set flixel-addons 3.3.2
      - name: Install hxvlc 2.2.1
        run: |
          haxelib install hxvlc 2.2.1
      - name: Install other libraries
        run: |
          haxelib git hxdiscord_rpc https://github.com/MAJigsaw77/hxdiscord_rpc
      - name: List Installed Haxelibs
        run: haxelib list
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
      - name: Compile
        run: haxelib run lime build Project.xml linux --app-version="4.0.0-${{ github.run_id}}"
      - name: Publish Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-release
          path: 'export/release/linux/bin'
  windows-release:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.4
      - name: Install hxcpp 4.3.2
        run: |
          haxelib install hxcpp 4.3.2
        shell: cmd
      - name: Install lime 8.2.2
        run: |
          haxelib install lime 8.2.2
        shell: cmd
      - name: Install openfl 9.4.1
        run: |
          haxelib install openfl 9.4.1
        shell: cmd
      - name: Install and Setup flixel 6.1.0
        run: |
          haxelib --never install flixel 6.1.0
          haxelib run lime setup flixel
          haxelib run lime setup
        shell: cmd
      - name: Install flixel tools and others
        run: |
          haxelib install flixel-tools 1.5.1
          haxelib set flixel-tools 1.5.1
          haxelib install flixel-ui 2.6.3
          haxelib set flixel-ui 2.6.3
          haxelib install flixel-addons 3.3.2
          haxelib set flixel-addons 3.3.2
        shell: cmd
      - name: Install hxCodec 3.0.2
        run: |
          haxelib install hxCodec 3.0.2
        shell: cmd
      - name: Install hxvlc 2.2.1
        run: |
          haxelib install hxvlc 2.2.1
        shell: cmd
      - name: Install other libraries
        run: |
          haxelib git hxdiscord_rpc https://github.com/MAJigsaw77/hxdiscord_rpc
        shell: cmd
      - name: List Installed Haxelibs
        run: haxelib list
        shell: cmd
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
      - name: Compile
        run: haxelib run lime build windows --app-version="4.0.0-${{ github.run_id}}"
      - name: Publish Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: export/release/windows/bin
  mac-release:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4
      - uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.4
      - run: |
          haxe -version
          haxelib install hxnodejs
          haxelib setup ~/haxelib
      - name: Install hxcpp 4.3.2
        run: |
          haxelib install hxcpp 4.3.2
      - name: Install lime 8.2.2
        run: |
          haxelib install lime 8.2.2
      - name: Install openfl 9.4.1
        run: |
          haxelib install openfl 9.4.1
      - name: Install and Setup flixel 6.1.0
        run: |
          haxelib --never install flixel 6.1.0
          haxelib run lime setup flixel
          haxelib run lime setup
      - name: Install flixel tools and others
        run: |
          haxelib install flixel-tools 1.5.1
          haxelib set flixel-tools 1.5.1
          haxelib install flixel-ui 2.6.3
          haxelib set flixel-ui 2.6.3
          haxelib install flixel-addons 3.3.2
          haxelib set flixel-addons 3.3.2
      - name: Install hxCodec 3.0.2
        run: |
          haxelib install hxCodec 3.0.2
      - name: Install hxvlc 2.2.1
        run: |
          haxelib install hxvlc 2.2.1
      - name: Install other libraries
        run: |
          haxelib git hxdiscord_rpc https://github.com/MAJigsaw77/hxdiscord_rpc
      - name: List Installed Haxelibs
        run: haxelib list
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
      - name: Compile
        run: haxelib run lime build mac --app-version="4.0.0-${{ github.run_id}}"
      - name: Publish Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mac-release
          path: export/release/macos/bin
